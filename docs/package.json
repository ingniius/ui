{
    "name": "@iueev/docs",
    "type": "module",
    "version": "0.0.3",
    "private": true,
    "scripts": {
        "prepare": "[ ! -d .astro ] && astro sync || true",
        "start": "astro preview",
        "dev": "astro dev",
        "build": "astro build",
        "lint": "eslint . --fix --cache --cache-location .cache/.eslintcache",
        "typecheck": "tsc --noEmit",
        "clean": "rimraf .astro .cache .turbo dist node_modules"
    },
    "devDependencies": {
        "@astrojs/starlight": "^0.34.4"
    }
}
