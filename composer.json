{
    "name": "ingniius/ui",
    "description": "A visual enhanced design system for Livewire.",
    "keywords": [
        "laravel",
        "livewire",
        "components",
        "ui"
    ],
    "type": "library",
    "license": "MIT",
    "authors": [
        {
            "name": "@omaucf",
            "email": "omaucf@pm.me"
        }
    ],
    "require": {
        "gehrisandro/tailwind-merge-laravel": "^1.3",
        "laravel/framework": "^10.0|^11.0|^12.0",
        "laravel/prompts": "^0.1|^0.2|^0.3",
        "livewire/livewire": "^3.5.19",
        "php": "^8.1",
        "unicon-rocks/unicon-laravel": "^0.4.2"
    },
    "require-dev": {
        "laravel/pint": "^1.13",
        "pestphp/pest": "^3.8",
        "phpstan/phpstan": "^2.1"
    },
    "autoload": {
        "psr-4": {
            "Vee\\": "src/"
        }
    },
    "autoload-dev": {
        "psr-4": {
            "Tests\\": "tests/"
        }
    },
    "scripts": {
        "lint": [
            "./vendor/bin/pint",
            "pnpm prettier ./stubs/** --write"
        ],
        "lint:test": [
            "./vendor/bin/pint --test",
            "pnpm prettier ./stubs/** --check"
        ],
        "test": [
            "@lint:test",
            "@test:unit",
            "@type:analyse"
        ],
        "test:unit": [
            "./vendor/bin/pest"
        ],
        "type:analyse": [
            "./vendor/bin/phpstan analyse --memory-limit=256M"
        ],
        "type:generate": [
            "./vendor/bin/phpstan analyse --generate-baseline --memory-limit=256M"
        ]
    },
    "extra": {
        "laravel": {
            "providers": [
                "Vee\\UiServiceProvider"
            ],
            "aliases": {
                "Vee": "Vee\\Ui"
            }
        }
    },
    "minimum-stability": "dev",
    "prefer-stable": true,
    "config": {
        "allow-plugins": {
            "pestphp/pest-plugin": true
        },
        "preferred-install": "dist",
        "sort-packages": true
    }
}
