import * as _colorMode from "./color-mode";
import * as _prose from "./prose";
import * as _ui from "./ui";

export const colorMode = {
  image: _colorMode.image,
};

export const prose = {
  img: _prose.img,
};

export const ui = {
  container: _ui.container,
  link: _ui.link,
  main: _ui.main,
};
