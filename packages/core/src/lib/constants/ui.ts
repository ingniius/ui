export const COLOR_MODE_KEYS = {
  color: "ui-color",
  theme: "ui-theme",
};

export const LOCALE_KEYS = ["en", "es", "fr", "pt"] as const;

export const ROUTER_KEYS = ["inertia", "tanstack"] as const;

export const STRATEGY_KEYS = ["join", "merge"] as const;
